#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

int main() {
    ios::sync_with_stdio(false); // hace que cin y cout sean mas rapidos
    cin.tie(nullptr); // evita que cout se vacie automaticamente antes de cada cin

    cout << "--- Divide y Venceras --- \n";

}